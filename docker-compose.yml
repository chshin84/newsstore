# 로컬 개발용 (Docker-only). compose가 `.` 경로를 자체 해석 → Git Bash 마운트 폴백 회피.
#   docker compose build
#   docker compose run --rm test        # 테스트 (Firestore 에뮬레이터에 붙어서 실행)
#   docker compose run --rm collect     # 1회 수집 (.env 필요: cp .env.example .env)
services:
  firestore-emulator:
    image: gcr.io/google.com/cloudsdktool/cloud-sdk:emulators
    command: gcloud emulators firestore start --host-port=0.0.0.0:8080 --project=test
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; socket.create_connection(('localhost',8080),2)"]
      interval: 2s
      timeout: 3s
      retries: 30

  test:
    build:
      context: .
      dockerfile: infra/Dockerfile
      args:                       # dev(pytest)+gcp(google-cloud-firestore) — 에뮬레이터 테스트용
        INSTALL_DEV: "true"
        INSTALL_GCP: "true"
    image: newsstore-test
    depends_on:
      firestore-emulator:
        condition: service_healthy
    environment:
      FIRESTORE_EMULATOR_HOST: "firestore-emulator:8080"
      GOOGLE_CLOUD_PROJECT: "test"
    volumes:
      - ".:/app"
    command: pytest -q

  collect:
    build:                        # 머리말 절차(docker compose build)만으로 실행 가능해야 한다 —
      context: .                  # 빌드 주체 없는 image 참조는 'image not found'로 깨진다.
      dockerfile: infra/Dockerfile
      args:
        INSTALL_DEV: "false"
        INSTALL_GCP: "true"       # 실 Firestore 접속(run_collect) — firestore 라이브러리 필수
        INSTALL_EMBED: "true"     # 임베딩 패스(gemini) — 백필 실행도 이 서비스로
    image: newsstore
    volumes:
      - ".:/app"
    env_file:
      - .env
    command: python -m newsstore.entrypoints.run_collect --force

  prices:                        # 가격 수집 — FMP(+Yahoo 폴백) 지수·환율·국채 → Firestore prices
    build:
      context: .
      dockerfile: infra/Dockerfile
      args:
        INSTALL_DEV: "false"
        INSTALL_GCP: "true"       # 실 Firestore 접속(prices 저장) — firestore 라이브러리 필수
    image: newsstore
    volumes:
      - ".:/app"
    env_file:
      - .env                      # FMP_API_KEY(백엔드 전용 비밀) + GOOGLE_CLOUD_PROJECT 등
    command: python -m newsstore.entrypoints.run_prices

  factors:                       # 팩터·펀더멘털 수집 — FMP → 계약 컬렉션(재무제표·비율·조정가·컨센서스·PIT유니버스)
    build:
      context: .
      dockerfile: infra/Dockerfile
      args:
        INSTALL_DEV: "false"
        INSTALL_GCP: "true"       # 실 Firestore 접속 — firestore 라이브러리 필수
    image: newsstore
    volumes:
      - ".:/app"
    env_file:
      - .env                      # FMP_API_KEY(백엔드 전용 비밀) + GOOGLE_CLOUD_PROJECT 등
    command: python -m newsstore.entrypoints.run_factors --cadence all
